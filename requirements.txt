# OtterForge — dépendances Python
# Installer avec : pip install -r requirements.txt

# Interface graphique
customtkinter

# Traitement d'images
Pillow

# Requêtes HTTP (API Scryfall)
requests

# Drag-and-drop de fichiers OS vers le canvas (images custom)
tkinterdnd2

# Upscaling IA (pipeline futur — optionnel pour l'instant)
# replicate
